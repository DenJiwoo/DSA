import random #Luis is so fucking gay !!!

randomNumbers = [random.randint (1, 99) for _ in range(10)] #Generates random numbers with random. with the range of 10

print(f"Array of random numbers are: {randomNumbers}")
